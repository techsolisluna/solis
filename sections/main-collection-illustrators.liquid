{{ 'section-main-collection-authors.css' | asset_url | stylesheet_tag }}

<div class="autorespage">
  <div class="autoresnav">
    <div style="margin-bottom:2.5em; text-align: center;">
      <span class="h3 autorestitulo">{{ page.title }}</span>
      <a href="/pages/autores" class="ilustralink"><span>> autores</span></a>
    </div>
  </div>

  <div class="autoresdiv">
    {% for collection in collections %}
      {% if collection.template_suffix == 'autor' and collection.metafields.autor.autor == 'ilustrador'or collection.metafields.autor.autor == 'autorilustrador' %}
        {% if collection.image %}
          <div class="autor-grid-item">
            <a href="{{collection.url}}">
              <img src="{{ collection.image | img_url: 'compact' }}" alt="{{ collection.title }}" class="desature" />
            </a>
            <p>{{ collection.title }}</p>
          </div>
        {% endif %}
      {% endif %}
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Lista de ilustradores"
}
{% endschema %}